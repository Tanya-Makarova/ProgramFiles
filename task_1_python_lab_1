#Необходимо ввести положительное число и проверить является ли оно палиндромом. Числовой палиндром – такое число, которое читается слева направо и справа налево одинаково. Например, число 1234321 – палиндром. Отдельные цифры также являются палиндромами.
def is_number_palindrome(number):
    # Преобразуем число в строку для удобства обработки цифр
    number_str = str(number)
    # Создаем стек (используем список как стек)
    stack = []
    # Помещаем все цифры числа в стек
    for d in number_str:
        stack.append(d)  # Добавляем каждую цифру в стек (push операция)

    # Сравниваем цифры из стека (обратный порядок) с исходными цифрами
    for d in number_str:
        # Извлекаем цифру из стека (последняя добавленная идет первой - LIFO)
        d_from_stack = stack.pop()  # Pop операция
        
        # Если цифры не совпадают - число не палиндром
        if d != d_from_stack:
            return False
    
    # Если все цифры совпали - число палиндром
    return True


# Ввод числа от пользователя
number = int(input("Введите положительное число: "))

# Проверка является ли число палиндромом
if is_number_palindrome(number):
    print(f"Число {number} является палиндромом!")
else:
    print(f"Число {number} НЕ является палиндромом!")
            
